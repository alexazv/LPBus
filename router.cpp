#include "router.h"

Router::Router()
{


}

void Router::buildRoute(Map*map){
    /*
     *
     while(still have passengers) do{
        add start node, finish node
        while(list of nodes not empty)do{
            choose next node:
            parse info to simplex
            get back result node

        add next node to list, add destination nodes to list
        go to next node in list
        passengers go down, passengers go up
        }
     }
     */
}
